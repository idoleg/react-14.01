import React from "react";
import './Profile.scss';

export const Profile = ({ }) =>
    (
        <div className={'profile'}>
            <div className={'userInfo'}>
                <h2>User profile </h2>
            </div>
        </div>
    );
